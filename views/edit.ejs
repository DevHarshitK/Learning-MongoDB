<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("includes/theme.ejs") %>
    <title>Edit</title>
</head>
<body>
    <%- include("includes/goback.ejs") %>
    <h3>Edit your message:</h3>
    <br>
    <form action="/chats/edit/<%= chat._id%>?_method=PUT" style="margin: 4px; padding: 4px;" method="post">
        <input type="text" value="From: <%= chat.from%>" disabled>
        <input type="text" value="To: <%= chat.to%>" disabled>
        <textarea name="message">
            <%= chat.message %>
        </textarea>
        <button class="btn btn-secondary">Submit</button>
    </form>
</body>
</html>